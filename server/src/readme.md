## to run the server project, please use the following script:

### set 3 environment variables before running the server:

$env:MONGO_USERNAME="<username>"
$env:MONGO_PASSWORD="<password>"
$env:JWT_PRIVATE_KEY="<privatekey>"

### run the server
npm run dev